module.exports = 
{
 	MemberShip_buyMembership_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_renewMembership_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_addManager_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_rmManager_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_assginKYCid_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_addWhitelistApps_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_rmWhitelistApps_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_addPenalty_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_readNotes_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_addNotes_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_addrIsMember_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_addrIsActiveMember_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_idIsMember_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_idIsActiveMember_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_queryCoreManager_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_addrToId_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_getMemberInfo_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_getIdInfo_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_pause_sanity(addr, jobObj)
        {
		return true;
	},
 	MemberShip_unpause_sanity(addr, jobObj)
        {
		return true;
        },
}
