module.exports = 
{
	GROUP_CONDITION: 'Optract',
	Optract_fillInEth_sanity(addr, jobObj) 
	{
		return true;
	},
	Optract_claimOptract_sanity(addr, jobObj) 
	{
		return true;
	},
	Optract_putOnStock_sanity(addr, jobObj) 
	{
		return true;
	},
	Optract_currentOwnerExercise_sanity(addr, jobObj) 
	{
		return true;
	},
	Optract_ethSellerWithdraw_sanity(addr, jobObj) 
	{
		return true;
	},
	Optract_queryOptionPrice_sanity(addr, jobObj) 
	{
		return true;
	},
        Optract_queryOrderPrice_sanity(addr, jobObj) 
	{
		return true;
	},
        Optract_queryOrderSize_sanity(addr, jobObj) 
	{
		return true;
	},
        Optract_queryOrderSize_sanity(addr, jobObj) 
	{
		return true;
	},
        Optract_queryOnStock_sanity(addr, jobObj) 
	{
		return true;
	},
        Optract_isFilled_sanity(addr, jobObj) 
	{
		return true;
	}
}
